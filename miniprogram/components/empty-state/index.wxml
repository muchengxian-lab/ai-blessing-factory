<view class="empty-wrap">
  <text class="empty-emoji">✨</text>
  <text class="empty-text">{{text}}</text>
  <button wx:if="{{showBtn}}" class="btn-primary empty-btn" bindtap="onTap">{{btnText}}</button>
</view>
