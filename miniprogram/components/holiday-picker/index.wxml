<scroll-view class="picker-scroll" scroll-x="true">
  <view class="picker-row">
    <view
      class="picker-item {{item.id === active ? 'picker-active' : ''}}"
      wx:for="{{holidays}}"
      wx:key="id"
      wx:for-item="item"
      bindtap="onSelect"
      data-id="{{item.id}}"
    >
      <image class="picker-icon" src="{{item.icon}}"></image>
      <text class="picker-name">{{item.name}}</text>
    </view>
  </view>
</scroll-view>
