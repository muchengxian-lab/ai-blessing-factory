<view class="style-grid">
  <view
    class="style-item {{item.id === active ? 'style-active' : ''}}"
    wx:for="{{styles}}"
    wx:key="id"
    wx:for-item="item"
    bindtap="onSelect"
    data-id="{{item.id}}"
  >
    <text class="style-emoji">{{item.emoji}}</text>
    <text class="style-name">{{item.name}}</text>
    <text class="style-desc">{{item.desc}}</text>
  </view>
</view>
