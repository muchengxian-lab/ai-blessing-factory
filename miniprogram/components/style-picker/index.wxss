.style-scroll { white-space: nowrap; }
.style-row { display: flex; padding: 8rpx 0; }
.style-item {
  display: flex; flex-direction: column; align-items: center; justify-content: center;
  width: 176rpx; min-width: 176rpx; height: 148rpx; margin-right: 16rpx;
  border-radius: var(--radius);
  background: var(--bg-card);
  border: 1rpx solid var(--glass-border);
  padding: 20rpx 8rpx;
  transition: all 0.25s;
}
.style-icon { width: 38rpx; height: 38rpx; margin-bottom: 6rpx; }
.style-name { font-size: 26rpx; font-weight: 600; margin-bottom: 2rpx; color: var(--text); }
.style-desc { font-size: 20rpx; color: var(--text-light); }
.style-active {
  border-color: var(--gold);
  background: rgba(240,192,96,0.12);
  box-shadow: 0 0 24rpx rgba(240,192,96,0.15);
}
.style-active .style-name { color: var(--gold); }
