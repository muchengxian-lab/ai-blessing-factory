# AI祝福工厂 — 技术实现路线

> v1.0 | 2026-06-02 | 微信AI小程序 | 50个功能点
> 个人主体 | 类目：工具 > 信息查询 | AI成长计划已加入
> 参考：周笺小记（同一技术栈，已验证可用）

---

## 一、技术栈

```
┌─────────────────────────────────────┐
│  前端：微信小程序原生                   │
│  - WXML + WXSS + JS                  │
│  - 无第三方框架，保持包体 < 2MB         │
│  - Canvas 2D（海报合成）              │
├─────────────────────────────────────┤
│  后端：微信云开发                      │
│  - 云函数（Node.js）                  │
│  - 云数据库（文档型 NoSQL）            │
│  - 云存储（海报图片）                  │
├─────────────────────────────────────┤
│  AI：腾讯混元大模型 API                │
│  - 文生文：hunyuan-turbo             │
│  - 文生图：hunyuan-image             │
│  - 免费额度：1亿Token + 1万张图        │
│  - wx.cloud.extend.AI（前端调用）     │
├─────────────────────────────────────┤
│  安全 & 分析                          │
│  - 微信内容安全 API（msgSecCheck）     │
│  - We 分析（免费）                     │
├─────────────────────────────────────┤
│  开发工具                             │
│  - 微信开发者工具                      │
│  - Git 版本控制                       │
└─────────────────────────────────────┘
```

**与周笺小记的差异：**
- ✅ 混元API用 wx.cloud.extend.AI 前端直调（已踩过坑，基础库≥3.7.1）
- ✅ 无复杂云函数（仅4个 vs 周笺小记13个）
- ✅ 无复杂偏好系统（用户不需选行业/模板/风格参数）
- ✅ 新增：Canvas海报合成（周笺小记未实现的长图导出）
- ✅ 新增：文生图API调用（周笺小记只用了文生文）

---

## 二、项目结构

```
miniprogram/
├── app.js                    # 入口：云开发初始化 + 登录
├── app.json                  # 页面注册 + TabBar + 窗口配置
├── app.wxss                  # 全局样式（CSS变量：6套风格配色）
├── pages/
│   ├── index/                # 首页-祝福生成
│   │   ├── index.js          #   节日选择 + 对象选择 + 风格选择
│   │   ├── index.wxml
│   │   ├── index.json
│   │   └── index.wxss
│   ├── preview/              # 海报预览页
│   │   ├── preview.js        #   文案切换 + 海报展示 + 分享/保存
│   │   ├── preview.wxml
│   │   ├── preview.json
│   │   └── preview.wxss
│   ├── history/              # 历史记录页
│   │   ├── history.js
│   │   ├── history.wxml
│   │   ├── history.json
│   │   └── history.wxss
│   └── mine/                 # 我的页
│       ├── mine.js
│       ├── mine.wxml
│       ├── mine.json
│       └── mine.wxss
├── components/
│   ├── holiday-picker/       # 节日选择器（横向滑动网格）
│   │   ├── index.js
│   │   ├── index.json
│   │   ├── index.wxml
│   │   └── index.wxss
│   ├── target-picker/        # 对象选择器（6选1按钮组）
│   ├── style-picker/         # 风格选择器（6选1卡片）
│   ├── blessing-card/        # 祝福文案卡片（文案展示+切换）
│   ├── poster-canvas/        # 海报Canvas渲染组件
│   ├── loading-state/        # 加载态组件（动画+随机文案）
│   └── empty-state/          # 空状态组件
├── utils/
│   ├── api.js                # 云函数 + AI调用封装（统一错误处理）
│   ├── prompt.js             # 节日×对象×风格 Prompt矩阵
│   ├── copywriting.js        # 文案轮换系统（加载文案/Toast/引导语）
│   ├── poster.js             # Canvas海报合成（背景+文案+装饰+水印）
│   ├── holidays.js           # 节日数据（日期/emoji/名称/倒计时计算）
│   ├── theme.js              # 6套风格CSS变量
│   └── config.js             # 全局常量（API参数/免费次数限制等）
├── images/                   # 图标 + 默认素材
│   ├── tab-icons/            # TabBar图标（home/history/mine）
│   └── default-poster-bg.png # 默认海报背景（AI生成失败时fallback）
cloudfunctions/
├── generateBlessing/
│   ├── index.js              # 核心：调混元文生文+文生图 → 返回文案+海报URL
│   └── package.json
├── listHistory/
│   ├── index.js
│   └── package.json
├── trackShare/
│   ├── index.js
│   └── package.json
└── getStats/
    ├── index.js
    └── package.json
```

---

## 三、核心实现细节

### 3.1 AI生成流程（generateBlessing）

```
输入：{ holiday, target, style, userId }
处理：
  1. 查Prompt矩阵 → 组装System Prompt（节日背景+对象关系+风格调性+约束）
  2. 调混元文生文（hunyuan-turbo）
     - 3次调用，temperature微调（0.8/0.9/1.0）
     - 生成3版不同文案
  3. 对3版文案逐一调msgSecCheck内容审核
     - 不通过的→再跑一次
  4. 调混元文生图（hunyuan-image）
     - Prompt：节日场景+风格配色+构图要求
     - 生成海报背景图，存入云存储
  5. 写入 blessings 表
  6. 返回：{ blessings: [文案1,2,3], posterBgUrl, posterStyleId }
容错：
  - 文生图失败 → 返回默认背景图URL（2套fallback）
  - 文生文失败 → 重试1次 → 仍失败返回 { code: 'AI_ERROR' }
  - 内容审核不通过 → 重试1次 → 仍不通过降级为安全模板文案
```

### 3.2 Canvas海报合成（utils/poster.js）

```javascript
// 海报规格：1080×1920px（3:4比例，朋友圈标准尺寸）
// 分层合成：
//   1. 背景层：AI生成的背景图或默认图
//   2. 文案层：祝福文字（白色/深色根据背景亮度自适应）
//   3. 装饰层：节日emoji + 装饰元素
//   4. 水印层：底部"AI生成 · AI祝福工厂"

function composePoster(canvasId, { bgImage, text, holiday, style }) {
  const ctx = wx.createCanvasContext(canvasId);
  const W = 1080, H = 1920;
  
  // 1. 背景
  ctx.drawImage(bgImage, 0, 0, W, H);
  
  // 2. 半透明遮罩（保证文字可读）
  ctx.fillStyle = 'rgba(0,0,0,0.3)';
  ctx.fillRect(60, 400, W-120, H-800);
  
  // 3. 文案排版
  ctx.setFontSize(48);
  ctx.setFillStyle('#FFFFFF');
  // ... 自动换行 + 居中对齐
  
  // 4. 节日emoji装饰
  // ... 
  
  // 5. 水印
  ctx.setFontSize(24);
  ctx.setFillStyle('rgba(255,255,255,0.5)');
  ctx.fillText('AI生成 · AI祝福工厂', W/2, H-60);
  
  ctx.draw();
}
```

### 3.3 文案切换逻辑（preview页）

```
用户进入preview页：
  - 携带参数：{ blessings: [文案1,2,3], posterBgUrl, posterStyleId }
  - 当前显示：第1版文案 + 对应海报
  - 点"◀ 换一个"→ 切到第2版文案 → Canvas重新合成（复用背景图+新文案）
  - 点"🔄 换一批"→ 重新调generateBlessing → 替换全部文案+海报
  - 点"🎨 换海报风格"→ 保持文案，重新调文生图 → 更新背景
```

### 3.4 混元API调用方式

```javascript
// 文生文（前端直调，wx.cloud.extend.AI）
const model = wx.cloud.extend.AI.createModel('hunyuan-turbo');
const res = await model.chat({
  messages: [
    { role: 'system', content: systemPrompt },
    { role: 'user', content: `节日：${holiday}，对象：${target}，风格：${style}` }
  ],
  temperature: 0.9,
  maxTokens: 500
});

// 文生图（云函数调用，Tencent Cloud SDK）
const res = await wx.cloud.callFunction({
  name: 'generateBlessing',
  data: { action: 'generateImage', prompt: imagePrompt }
});
```

### 3.5 Prompt矩阵设计（utils/prompt.js）

```javascript
const PROMPT_MATRIX = {
  holiday: {
    '父亲节': '6月第三个周日，感恩父亲的节日。传统：送礼物、陪伴、表达感谢。',
    '端午节': '农历五月初五，纪念屈原。传统：吃粽子、赛龙舟、挂艾草。',
    '七夕': '农历七月初七，中国情人节。传统：表达爱意、约会、送礼物。',
    '中秋节': '农历八月十五，团圆节。传统：赏月、吃月饼、家人团聚。',
    '春节': '农历正月初一，中国最重要的传统节日。传统：拜年、发红包、团圆。',
    '教师节': '9月10日，感谢老师的节日。传统：送祝福、表达感恩。',
    '母亲节': '5月第二个周日，感恩母亲。传统：送礼物、表达感谢。',
    '生日': '个人出生纪念日。传统：庆祝、祝福、送礼物。',
  },
  target: {
    '长辈': '关系特征：尊敬、感恩。语气要温暖真诚，不能太随意或太正式。',
    '领导': '关系特征：尊重、得体。语气要正式但不刻板，表达感谢和敬意。',
    '朋友': '关系特征：平等、轻松。语气可以幽默风趣，有共同记忆更好。',
    '恋人': '关系特征：亲密、浪漫。语气要甜蜜真诚，有专属感。',
    '老师': '关系特征：尊敬、感恩。语气要真诚温暖，表达感谢和祝福。',
    '通用': '关系特征：中性、得体。语气要适当、不过不失。',
  },
  style: {
    '温情': '调性：走心、真诚、触动情感。用具体细节表达，避免空洞套话。',
    '正式': '调性：得体、尊重、商务礼仪。结构清晰，措辞准确，不卑不亢。',
    '幽默': '调性：轻松、有趣、不油腻。可以有梗，但不能冒犯或过于随意。',
    '文艺': '调性：诗意、优美、有文采。可用比喻、排比等修辞，但不做作。',
    '古风': '调性：典雅、诗词化。可用四字短语、对仗句式、古典意象。',
    '简约': '调性：干净、直接、不啰嗦。简短有力，一句话说到心里。',
  }
};
```

---

## 四、数据库设计

```javascript
// blessings 表
{
  _id: 'auto',
  userId: 'OPENID',      // 用户标识
  holiday: '父亲节',       // 节日
  target: '长辈',         // 对象
  style: '温情',          // 风格
  content: [              // 3版文案
    '爸，平时话少...',
    '小时候您牵我...',
    '不管多大...'
  ],
  posterBgUrl: 'cloud://...',  // 海报背景图
  posterStyleId: 1,      // 海报模板ID
  createdAt: timestamp
}

// user_settings 表
{
  _id: 'auto',
  userId: 'OPENID',
  remindEnabled: true,   // 节日提醒开关
  createdAt: timestamp,
  updatedAt: timestamp
}

// 索引
// blessings: { userId: 1, createdAt: -1 }  ← 历史列表查询
```

---

## 五、开发依赖顺序

```
Day 1    基础搭建
          ├── app.js / app.json / app.wxss（含6套CSS变量+TabBar配置）
          ├── 微信云开发环境 + 数据库建表（blessings / user_settings）
          ├── 混元API密钥配置（wx.cloud.extend.AI验证）
          ├── utils：api.js / config.js / holidays.js / theme.js / copywriting.js
          └── 占位页面（index/preview/history/mine空壳）

Day 2-3  核心生成链路 ★
          ├── prompt.js（节日×对象×风格 Prompt矩阵）
          ├── generateBlessing 云函数（调混元文生文+文生图）
          ├── index 页面（节日/对象/风格选择器）
          └── preview 页面（文案展示+切换+加载态）

Day 3-4  海报 & 分享
          ├── poster.js（Canvas海报合成：背景+文案+装饰+水印）
          ├── Canvas性能优化（避免频繁重绘）
          ├── 一键分享（微信转发/保存到相册）
          └── loading-state / empty-state 组件

Day 4-5  历史 & 设置 & 测试
          ├── listHistory 云函数
          ├── history 页面
          ├── mine 页面（节日提醒+关于+反馈）
          ├── msgSecCheck 内容审核接入
          ├── 边界测试（无网络/生成失败/超长文案/内容违规）
          └── 提审材料准备
```

---

## 六、关键技术决策

| 决策 | 选择 | 理由 |
|------|------|------|
| 前端框架 | 微信原生 | 包体小、加载快、周笺小记已验证 |
| AI调用方式 | wx.cloud.extend.AI前端直调 | 更快（省一次云函数往返）、周笺小记已踩坑 |
| AI模型-文案 | 混元 Turbo | 中文优化好、免费、够用 |
| AI模型-图片 | 混元文生图 | 1万张免费额度、中文理解好 |
| 海报合成 | Canvas 2D | 小程序原生、无需第三方 |
| 海报规格 | 1080×1920（3:4） | 朋友圈标准尺寸、最佳展示效果 |
| 数据库 | 云开发文档型 | 2张表简单够用 |
| 主题系统 | CSS变量 | 6套配色切换无闪烁 |
| Tab图标 | 纯SVG | 周笺小记经验：emoji跨平台不一致 |

## 七、风险与备选

| 风险 | 备选方案 |
|------|----------|
| 混元文生图额度用完（1万张） | Fallback预设模板图（6套×3张=18张默认背景） |
| 混元文生文超时 | 降级到预设祝福语库（每种节日×对象×风格预存5条） |
| wx.cloud.extend.AI不可用 | 改用云函数调混元（周笺小记的generateReport方案） |
| 微信审核不通过（类目） | 改选"工具>信息查询"类目，去掉"AI生成"字样改为"智能祝福" |
| Canvas Android适配问题 | 真机测试优先覆盖Android主流机型 |
| 生成次数过多烧Token | 单用户限制：每天10次免费，超额提示第二天再来或付费 |
